[package]
name = "sysctl-conf"
version = "0.1.0"
edition = "2021"
description = "Parser library for files written in the sysctl.conf(5) syntax, with schema validation"
license = "MIT"
publish = false

[dependencies]
serde = "1"
serde_json = "1"

[profile.release]
strip = true
